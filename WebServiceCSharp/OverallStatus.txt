```Test:
`````1. CalculatorTests:
      `  -------BasicCalculatorTests
	  `	 -------BasicCalculatorTestsWithURI
	  `	 -------BasicCalculatorTestsWithoutHeaders
	  `	 -------BasicCalculatorTestsWithRawData (Data will be given in TC method itself)
	  `	 -------SerializationTests
	  `	 -------DeSerializationTests
	  `  -------CheckStatusCodeOfResponse
	  `  -------CompareActualAndExpectedResponse
	  `
	  `
`````2. DataValidation:
	  `  -------DataValidationTests
	  `  -------DataValidationTestsWithURI
      `	 -------AddURLParametersToURL(string keyValue)
	  `	 -------AddURLParametersToURL(Dictionary<String,String> parametersValue)
	  `  -------CheckStatusCodeOfResponse
	  `  -------CompareActualAndExpectedResponse
	  `
	  `
`````3. NavigationTests:
	  `  -------BasicNavigationTests
	  `  -------BasicNavigationTestsWithURI
      `	 -------BasicNavigationTestsWithoutHeaders
	  `  -------CheckStatusCodeOfResponse
	  `  -------CompareActualAndExpectedResponse
	  `
	  `
`````4. OtherTests:
	  `  -------SerializationJSON
	  `  -------DeSerializationJSON
	  `  -------CheckStatusCodeOfResponse
	  `  -------CompareActualAndExpectedResponse
	  `
	  `
```Test_
`````````_ExceptionTests:
`````````````````````````1.CalculatorTestsException
`````````````````````````2.DataValidationTestsException
`````````````````````````3.NavigationTestsException





Overall Scenarios Tested So Far:
                                1. Tests that take URI from ExcelSheet using Identifier.
								2. Tests that take Headers,POST Body from ExcelSheet using Identifer.
								3. Tests that takes no data from ExcelSheet (i.e, data such as URI,Headers,POST Body will be given in Test method itself).
								4. Adding Headers in Tests instead of getting Headers from ExcelSheet.
								5. Adding URL parameters in Tests instead of getting URL parameters frm ExcelSheet.
								6. Tests that takes Request Body from ExcelSheet (Raw Data in the format of xml/json/text).
								7. Tests that refer a .xml/.json/.txt file where Request Body informations are saved respectively. 
								8. Tests that takes Expected Response Body (Raw Data in the format of xml/json/text) from ExcelSheet.
								9. Tests that refer a .xml/.json/.txt file where Response Body informations are saved respectively.
							   10. Gets the Actual Response Body in the format of xml/json.
							   11. Compare the Actual and Expected Response Body are being the same.
							   12. Verify the Status code of the Response.
							   13. Tests that verifies wheather correct Exception is occuring.
							   14. Serialization -xml/json.
							   15. DeSerialization -xml/json.
							   16. ExcelSuiteRunner -run the whole tests in a ExcelSheet.



